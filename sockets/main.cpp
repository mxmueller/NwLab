#include <iostream>


// Bitte lesen Sie dazu die Seiten 6-15 im Tutorial und
// probieren das aus, insbesondere die Umwandlung einer als Name (localhost) oder
// Adresse (127.0.0.1) angegebenen Adresse in eine struct sockaddr bzw. struct sockaddr_in.
// Das brauchen wir nächstes Mal als Ausgangsbasis für unsere ersten "echten" Server und Clients.

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
